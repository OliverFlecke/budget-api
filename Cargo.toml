[package]
name = "budget-api"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = { version = "0.6.1" }
sqlx = { version = "0.6.2", features = [
    'runtime-tokio-rustls',
    'postgres',
    'offline',
    'uuid',
] }
tokio = { version = "1.23.0", features = ["full"] }
